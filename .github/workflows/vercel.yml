name: Deploy Checks

on:
  push:
    branches:
      - main

  pull_request:

jobs:
  verify:

    runs-on: ubuntu-latest

    steps:

      - uses: actions/checkout@v4

      - name: Validate HTML
        run: |
          echo "Checking project..."

      - name: List files
        run: ls -R

      - name: Build Complete
        run: echo "Simulator Ready"